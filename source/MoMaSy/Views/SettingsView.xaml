<UserControl x:Class="outcold.MoMaSy.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Button Click="GoToTransactionTypesView"  Style="{StaticResource LinkButtonMenu}">Transaction Types</Button>
            <Button Click="GoToAccountsView"  Style="{StaticResource LinkButtonMenu}">Accounts</Button>
            <Button Click="GoToCurrencyTypesView"  Style="{StaticResource LinkButtonMenu}">Currency Types</Button>
        </StackPanel>
        
        <ContentControl Grid.Column="1" x:Name="Frame">
            
        </ContentControl>
    </Grid>
</UserControl>
